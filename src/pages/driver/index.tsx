import ProfileInfo from "@/components/common/ProfileInfo";
import Home from "@/components/driver/Home";
import MenuDriver from "@/components/driver/MenuDriver";
import React from "react";

const Driver = () => {
    return (
        <div>
            <ProfileInfo />
            <Home />
            <MenuDriver />
        </div>
    );
};

export default Driver;
